# Simple Login Application Environment Configuration
# This is an OPTIONAL configuration file for advanced users

# STUDENTS: You don't need this file! The app works with default settings.
# DEVELOPERS: Copy this to the root as `.env` if you want to customize settings.

# JWT Configuration
AUTH_JWT_SECRET=your-super-secure-jwt-secret-key-at-least-64-characters-long

# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/simple_login

# Java Configuration (macOS with Homebrew)
JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home

# Frontend Configuration
VITE_API_BASE_URL=http://localhost:8080/api

# Development Settings
NODE_ENV=development
SPRING_PROFILES_ACTIVE=dev

# -----------------------------
# Usage Instructions
# -----------------------------
# 1. Copy this file to the root directory as `.env`
# 2. Update the values as needed for your environment
# 3. The application will automatically load these values
# 4. Never commit `.env` to git (it's in .gitignore)
